# `auto_workflow` Module Public API

This document enumerates the full public API of the `auto_workflow` module.

## Functions
- `run(logger: &logger::Logger, cli_args: CliArgs) -> Result<(), AppError>`